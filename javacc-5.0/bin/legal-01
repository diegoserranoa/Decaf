class Program {
  int a, b, c, d;
  int e[5];
  boolean bool;

  int hola(int a, boolean b, int c){
      return;
  }

  void main(){
  	a = c + b + d;
  	bool = false;
  	if (5 < 10){
  		a = 5;
  	}

  	if (true || bool){
  		a = 5;
  	}

    hola(5, true, a);
  }

  

  void noReturn(){
      return;
  }
}
