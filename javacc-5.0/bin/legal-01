class Program {
  int a, b, c, d;
  int e[5];
  boolean bool;

  int hola(int a){
      return;
  }

  void main(){
  	a = c + b + d;
  	bool = false;
  	if (5 < 10){
  		a = 5;
  	}

  	if (true || bool){
  		a = 10;
  	}

    hola(a);
  }

  

  void noReturn(){
      return;
  }
}
